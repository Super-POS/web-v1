# Copy to .env for local `ng build` / Docker build.
# Docker Compose passes the same values via build args + ENV in the Dockerfile.
API_BASE_URL=http://localhost:9003/api
FILE_BASE_URL=http://localhost:9006/
SOCKET_URL=http://localhost:9003
